export enum SlashCommand {
  AUTHORIZE = 'authorize'
}

export enum ButtonCommand {
  AUTHORIZE = 'authorize',
  GET_RESULTS = 'get_results',
  REFRESH = 'refresh',
  SHARE = 'share'
}

export enum ModalCommand {
  AUTHORIZE = 'authorize'
}

export enum Authorize {
  SEND_DM_MODEL = 'send_dm.model'
}

export enum TextCommnad {
  OAUTH_URL = 'oauth_url'
}

export enum Av5ja {
  COOP_HISTORY = 'coop_history',
  COOP_HISTORY_RESULT = 'coop_history_result'
}
